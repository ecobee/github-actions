-   id: remove-crlf
    # This hook has been copied mostly as-is from 'Lucas-C/pre-commit-hooks'.
    # mainly to maintain a copy of it in our controlled repo. The copied
    # `remove_crlf.py` script does not present any threats.
    name: remove CRLF end-lines
    description: "Replace CRLF end-lines by LF ones before committing"
    entry: remove_crlf
    language: python
    types: [text]
    # Commenting out `stages` and `minimum_pre_commit_version` to allow
    # the hook to run on older version of pre-commit. Seems to work.
    # stages: [pre-commit, pre-push, pre-merge-commit]
    # minimum_pre_commit_version: "3.2.0"

-   id: bazel-buildifier
    # This hook was first inspired by https://github.com/brymck/pre-commit-buildifier/
    # but we decided to ditch the auto-installation and ship an hard-coded binary
    # instead.
    name: bazel buildifier
    entry: pre_commit_hooks/run-buildifier.sh
    language: script
    files: |
      (?x)^(
        BUILD|
        .*/BUILD|
        WORKSPACE|
        .*\.(bzl|sky|star)|
        (BUILD|WORKSPACE)\..*\.(bazel|oss)
      )$
    description: Runs Buildifier on Bazel build and workspace files.
    #minimum_pre_commit_version: '1'
